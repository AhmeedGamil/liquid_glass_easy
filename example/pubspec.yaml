
name: liquid_glass_easy_example
description: "Example app demonstrating the usage of liquid_glass_easy package."
publish_to: "none"

environment:
  sdk: ">=3.6.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^2.3.0
  # Gallery picker for the shader lab's backdrop.
  image_picker: ^1.1.2
  liquid_glass_easy:
    path: ../
  # Experimental shader lab; lives outside the published package.
  kitchen_glass_lab:
    path: ../kitchen/experimental

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    # Outline + filled SVG pairs driving the "Scaffold + SVG Nav" demo.
    - assets/icons/
