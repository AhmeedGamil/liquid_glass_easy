import 'package:flutter/material.dart';

import 'aurora/aurora_app.dart';

/// Aurora — the package living inside a real app.
///
/// Run it with: `flutter run -t lib/aurora_main.dart`
void main() => runApp(const AuroraApp());
