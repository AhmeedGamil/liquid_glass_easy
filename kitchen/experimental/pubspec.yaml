name: kitchen_glass_lab
description: "Experimental liquid-glass shader lab: a merged multi-lobe glass surface on Impeller, with live tuning."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.6.2 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  shaders:
    - shaders/kitchen_liquid_glass.frag
